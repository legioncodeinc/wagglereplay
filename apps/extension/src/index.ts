/**
 * Placeholder entry point for @waggle/extension.
 *
 * Full implementation lands in prd-003. This package exists now so the
 * workspace boots and downstream packages have a stable import target to
 * build against.
 */

export const EXTENSION_PLACEHOLDER = 'apps/extension: full implementation lands in prd-003';

export function isPlaceholder(): boolean {
  return true;
}
