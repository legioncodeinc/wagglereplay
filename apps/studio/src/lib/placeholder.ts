/**
 * Placeholder for apps/studio.
 *
 * The film-strip, step detail, narration, and render UI described in
 * library/requirements/backlog/prd-005-studio-app/ land in prd-005. This
 * page exists now so the SvelteKit app boots and the monorepo has a stable
 * place for `waggle studio` / `waggle record` (prd-004, prd-005) to launch.
 */
export const STUDIO_PLACEHOLDER = 'Waggle Studio: full implementation lands in prd-005';

export function isPlaceholder(): boolean {
  return true;
}
