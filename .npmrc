# pnpm strict by default (no phantom deps: a package must declare anything it
# imports). Peer-dependency strictness is relaxed because the SvelteKit
# ecosystem (svelte / @sveltejs/kit / vite / svelte-check) moves fast enough
# that pinning every transitive peer exactly is more friction than value here.
strict-peer-dependencies=false
auto-install-peers=true
