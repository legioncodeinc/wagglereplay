{
  "name": "@waggle/ir",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Walkthrough IR: TypeScript types and zod schemas (a strict superset of the Puppeteer Replay user-flow schema, ADR-001), validation with precise error paths, Chrome Recorder import, Puppeteer Replay export, the immutable version writer, and coordinate projection.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@puppeteer/replay": "^4.0.2",
    "@types/node": "^26.2.0",
    "fast-check": "^4.9.0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "license": "AGPL-3.0-or-later"
}
