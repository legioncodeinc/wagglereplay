/**
 * Placeholder entry point for packages/ir.
 *
 * The Walkthrough IR types, zod schemas, immutable version writer, and
 * Chrome Recorder / @puppeteer/replay import-export helpers described in
 * library/knowledge/private/waggle/walkthrough-ir-and-project-format.md are
 * built out in prd-002. This package exists now so the workspace boots and
 * downstream packages have a stable import target to build against.
 */

export const IR_PACKAGE_PLACEHOLDER = 'packages/ir: full implementation lands in prd-002';

export function isPlaceholder(): boolean {
  return true;
}
