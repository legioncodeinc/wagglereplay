/**
 * Placeholder entry point for @waggle/narrate.
 *
 * Full implementation lands in prd-006. This package exists now so the
 * workspace boots and downstream packages have a stable import target to
 * build against.
 */

export const NARRATE_PACKAGE_PLACEHOLDER = 'packages/narrate: full implementation lands in prd-006';

export function isPlaceholder(): boolean {
  return true;
}
