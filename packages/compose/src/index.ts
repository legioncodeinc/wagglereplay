/**
 * Placeholder entry point for @waggle/compose.
 *
 * Full implementation lands in prd-007. This package exists now so the
 * workspace boots and downstream packages have a stable import target to
 * build against.
 */

export const COMPOSE_PACKAGE_PLACEHOLDER = 'packages/compose: full implementation lands in prd-007';

export function isPlaceholder(): boolean {
  return true;
}
