/**
 * Placeholder entry point for @waggle/replay.
 *
 * Full implementation lands in prd-009. This package exists now so the
 * workspace boots and downstream packages have a stable import target to
 * build against.
 */

export const REPLAY_PACKAGE_PLACEHOLDER = 'packages/replay: full implementation lands in prd-009';

export function isPlaceholder(): boolean {
  return true;
}
