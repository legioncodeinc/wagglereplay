/**
 * Placeholder entry point for @waggle/plugin-remotion.
 *
 * Full implementation lands in prd-014. This package exists now so the
 * workspace boots and downstream packages have a stable import target to
 * build against.
 */

export const REMOTION_PLUGIN_PLACEHOLDER = 'plugins/remotion: full implementation lands in prd-014';

export function isPlaceholder(): boolean {
  return true;
}
